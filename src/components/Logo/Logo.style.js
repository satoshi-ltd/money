import StyleSheet from 'react-native-extended-stylesheet';

export const style = StyleSheet.create({
  image: {
    height: '$spaceXL * 1.2',
    marginBottom: '$spaceM',
    width: '$spaceXL * 4',
  },
});
