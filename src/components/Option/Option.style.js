import StyleSheet from 'react-native-extended-stylesheet';

const style = StyleSheet.create({
  card: {
    height: '$optionSize',
    gap: '$spaceXXS',
    width: '$optionSize',
  },
});

export { style };
